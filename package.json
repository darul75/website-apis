{
  "name": "website-apis",
  "scripts": {
    "build": "browserify -t reactify app.js -o public/bundle.js",
    "start": "npm run build && node server.js"
  },
  "engines": {
    "node": "0.12.x"
  },
  "devDependencies": {
    "browserify": "^7.0.0",
    "reactify": "^0.17.1"
  },
  "dependencies": {
    "express": "^4.10.6",
    "node-jsx": "^0.12.4",
    "react": "^0.12.1",
    "jquery": "2.1.3"
  }
}
